import java.util.Scanner;

public class Test5 {
    public static void main(String[] args) {

        /*
         * ask user to create a password.
         * it should have at least
         * 1 special char
         * 1 uppercase
         * 1 lowercase
         * 1 digit
         * the length of password should be at least 8 characters
         */

        /*
        Scanner scanner=new Scanner(System.in);
        System.out.println("Lutfen password giriniz");
        String password = scanner.nextLine();


        boolean sifreDogruMu = true;

        if (!password.contains(CharSequence'')){
            System.out.println("it should have at least 1 special char ");
            sifreDogruMu =false;
        }
        if (!password.contains(password.toUpperCase())){
            System.out.println("it should have at least 1 uppercase ");
            sifreDogruMu =false;
        }
        if (!password.contains(password.toLowerCase())){
            System.out.println("it should have at least 1 lowercase ");
            sifreDogruMu =false;
        }


        if (!password.contains(Character.isDigit())){


        }
        if (!(password.length()>=8)){
            System.out.println("the length of password should be at least 8 characters ");
            sifreDogruMu =false;
        }
        if (sifreDogruMu){
            System.out.println("the password is correct ");
        }


        */
    }
}
